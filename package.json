{
  "name": "live_cast",
  "version": "1.1.0",
  "description": "Stream URLs (e.g. HLS) to Chromecast using yt-dlp and VLC",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  },
  "private": true
}
